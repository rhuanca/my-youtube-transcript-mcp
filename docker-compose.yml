services:
  youtube-transcriber-mcp:
    build: .
    ports:
      - "8000:8000"
    environment:
      - YOUTUBE_API_KEY=${YOUTUBE_API_KEY}
      - CAPTION_LANGUAGE=${CAPTION_LANGUAGE:-en}
      - WHISPER_MODEL=${WHISPER_MODEL:-small}
